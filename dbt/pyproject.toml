[project]
name = "dbt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mitul Neema", email = "mitulneema08@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "dbt-bigquery==1.12.0",
    "dbt-core==1.11.11",
]

[project.scripts]
dbt = "dbt:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
